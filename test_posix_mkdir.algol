# Test POSIX mkdir and rmdir operations
fn main() -> i64 {
  result: i64 = mkdir("test_directory", 0755);

  if (result == -1) {
    return 1;
  }

  result = rmdir("test_directory");

  if (result == -1) {
    return 2;
  }

  return 0;
}