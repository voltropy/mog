// host-stop-async.mog
// Host timeout stress case: never-completing suspended async wait.

requires process

async fn main() -> int {
    while 1 {
        sleep_ms: int = await process.sleep(30000);
        if sleep_ms < 0 {
            return 0;
        }
    }
    return 0;
}
