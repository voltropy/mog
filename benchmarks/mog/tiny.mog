fn fib(n: i64) -> i64 {
  if (n <= 1) { return n; }
  return fib(n - 1) + fib(n - 2);
}

fn factorial(n: i64) -> i64 {
  if (n <= 1) { return 1; }
  return n * factorial(n - 1);
}

fn main() -> i64 {
  println_i64(fib(10));
  println_i64(factorial(10));
  return 0;
}
