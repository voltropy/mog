# Test basic POSIX open/write/close operations
fn main() -> i64 {
  fd: i64 = open("test_posix.txt", O_CREAT | O_WRONLY, 0644);

  if (fd == -1) {
    return 1;
  }

  write(fd, "Hello POSIX from AlgolScript!\n", 32);
  close(fd);

  return 0;
}